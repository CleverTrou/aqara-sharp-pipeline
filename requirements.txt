# SHARP Pipeline server dependencies
flask>=3.1.3
pyyaml>=6.0
requests>=2.34.1

# SHARP itself is installed separately via:
#   git clone https://github.com/apple/ml-sharp.git
#   cd ml-sharp && pip install -r requirements.txt
